{
  "name": "ght-save-server",
  "version": "1.0.0",
  "description": "Local save server for timetable (writes trens.json)",
  "main": "save-server.js",
  "scripts": {
    "start": "node save-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
{
  "name": "ght-timetable-save-server",
  "version": "1.0.0",
  "description": "Local save server for timetable (writes trens.json)",
  "main": "save-server.js",
  "scripts": {
    "start": "node save-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
